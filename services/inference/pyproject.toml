[tool.pytest.ini_options]
# Tests import the service as ``app`` — put the service root on sys.path.
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
