# Inference — LightGBM model serving. The model artifacts are baked into the
# image, so there is no runtime volume or external dependency.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: inference
  namespace: netguard
  labels: { app: inference, app.kubernetes.io/part-of: netguard }
spec:
  replicas: 1
  selector:
    matchLabels: { app: inference }
  template:
    metadata:
      labels: { app: inference }
    spec:
      imagePullSecrets: [{ name: registry-secret }]
      containers:
        - name: inference
          image: ghcr.io/yasmine1542/netguard-inference:latest
          imagePullPolicy: Always
          envFrom:
            - configMapRef: { name: netguard-config }
          ports:
            - { containerPort: 8000, name: http }
          resources:
            requests: { cpu: 200m, memory: 256Mi }
            limits: { cpu: "1", memory: 512Mi }
          readinessProbe:
            httpGet: { path: /readyz, port: 8000 }
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /healthz, port: 8000 }
            initialDelaySeconds: 20
            periodSeconds: 20
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: inference
  namespace: netguard
spec:
  selector: { app: inference }
  ports:
    - { name: http, port: 8000, targetPort: 8000 }
