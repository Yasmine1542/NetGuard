// Legacy component — replaced by page-level summary cards
import type { Prediction } from "../hooks/useWebSocket";
export default function StatBanner(_: { predictions: Prediction[] }) {
  return null;
}
