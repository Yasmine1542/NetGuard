// Legacy component — replaced by ThreatDetectionPage
import type { Prediction } from "../hooks/useWebSocket";
export default function ThreatLevel(_: { predictions: Prediction[] }) {
  return null;
}
